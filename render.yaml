services:
  - type: web
    name: garment-designer-demo
    runtime: python
    plan: free
    buildCommand: "true"
    startCommand: "python server.py"
    envVars:
      - key: OPENAI_API_KEY
        sync: false
